let arr = ['эми', 'жаба', 'мага', 'турлаев', 'садиков', 'магамадов', 'ахмад', 'аюб', 'асет', 'арун', 'дина']
for (let i = 0; i <= arr.length; i++) {
    console.log(arr[i])
};
let i = 0;
while (i < arr.length) {
    console.log(arr[i])
    i ++
}